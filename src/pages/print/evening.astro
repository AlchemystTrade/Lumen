---
import library from '../../content/library.json';

const eveningContent = library.filter(item => item.period === 'evening');
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Lumen - Evening Collection (Print Version)</title>
    <style>
        body {
            font-family: "Georgia", serif;
            line-height: 1.5;
            color: #000;
            background: #fff;
            max-width: 800px;
            margin: 40px auto;
            padding: 20px;
        }
        h1 {
            font-size: 2.5rem;
            border-bottom: 2px solid #000;
            padding-bottom: 10px;
            margin-bottom: 30px;
        }
        .intro {
            font-style: italic;
            margin-bottom: 40px;
            font-size: 1.2rem;
        }
        article {
            margin-bottom: 50px;
            page-break-inside: avoid;
        }
        h2 {
            font-size: 1.8rem;
            margin-bottom: 5px;
        }
        .meta {
            font-size: 0.9rem;
            margin-bottom: 20px;
            color: #333;
        }
        .note {
            margin-bottom: 20px;
            font-size: 1.1rem;
        }
        .questions {
            background: #eee;
            padding: 15px;
            border: 1px solid #ccc;
        }
        .questions h3 {
            margin-top: 0;
            font-size: 1rem;
            text-transform: uppercase;
        }
        @media print {
            body { margin: 0; padding: 0; width: 100%; }
            .questions { background: transparent; border: 1px solid #000; }
        }
    </style>
</head>
<body>
    <h1>Lumen: Evening</h1>
    <p class="intro">
        The evening collection offers works that are calm, deep, and atmospheric. 
        Best suited for the transition into night.
    </p>

    {eveningContent.map(item => (
        <article>
            <h2>{item.title}</h2>
            <p class="meta">Source: {item.source} · License: {item.license}</p>
            <p class="note">{item.note}</p>
            <div class="questions">
                <h3>Reflection Questions</h3>
                <ul>
                    <li>What did you notice that you hadn’t before?</li>
                    <li>Why do you think this happens?</li>
                    <li>How is this connected to everyday life?</li>
                </ul>
            </div>
            <p style="font-size: 0.8rem; margin-top: 10px;">Source URL: {item.url}</p>
        </article>
    ))}
    
    <footer style="margin-top: 50px; font-size: 0.8rem; border-top: 1px solid #000; padding-top: 10px;">
        Generated by Lumen Library — A quiet, non-commercial video library.
    </footer>
</body>
</html>
